{
  "name": "@mail/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm run db:local",
    "dev": "wrangler dev --env local --port 8787",
    "build": "tsc -p tsconfig.json && wrangler deploy --dry-run --env=\"\"",
    "typecheck": "tsc -p tsconfig.json",
    "db:local": "npm run db:local:control && npm run db:local:organization:1 && npm run db:local:organization:2 && npm run db:local:organization:3",
    "db:local:control": "wrangler d1 migrations apply CONTROL_DB --env local --local",
    "db:local:organization:1": "wrangler d1 migrations apply LOCAL_ORGANIZATION_DB_1 --env local --local",
    "db:local:organization:2": "wrangler d1 migrations apply LOCAL_ORGANIZATION_DB_2 --env local --local",
    "db:local:organization:3": "wrangler d1 migrations apply LOCAL_ORGANIZATION_DB_3 --env local --local",
    "deploy": "npm run deploy --prefix ../..",
    "deploy:worker:release": "wrangler deploy --env=\"\""
  },
  "dependencies": {
    "@mail/domain": "*",
    "drizzle-orm": "^0.45.2",
    "fast-xml-parser": "^5.10.1",
    "fflate": "^0.8.3",
    "hono": "^4.8.12",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  }
}
