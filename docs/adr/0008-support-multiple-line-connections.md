# Support multiple LINE connections

Each Organization may authorize multiple LINE Connections. Every Automation Rule selects the LINE Connection used to send notifications, and every LINE Destination List is scoped to one connection so destination identifiers and credentials cannot be mixed across Messaging API channels.
