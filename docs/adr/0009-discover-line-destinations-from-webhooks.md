# Discover LINE destinations from webhooks

Mail Automation will discover LINE users, groups, and rooms from signature-verified webhook events for each LINE Connection. Newly discovered destinations appear unregistered in the GUI until an authorized member names them and adds them to a LINE Destination List; arbitrary destination IDs are not treated as trusted contacts.
