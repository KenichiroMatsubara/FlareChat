# Extract event details with AI

Mail Automation will use AI to extract structured Event Details from each Source Message and its attachments. Automation Rules provide fixed values and defaults, such as the time zone and duration, so incomplete messages can still be handled deterministically without human approval.
