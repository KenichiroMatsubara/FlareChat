# Use the automation inbox for all Google actions

The Organization's one Automation Inbox reads Gmail, owns Scheduled Events in Google Calendar, and stores source attachments in its Google Drive. Member Addresses are outbound Calendar invitation destinations only; members are never asked for Google OAuth and Mail Automation never requests access to their mailboxes.
