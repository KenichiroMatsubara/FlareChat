# Use one automation inbox per organization

Each Organization has exactly one Automation Inbox from which all Source Messages are read. Member Addresses and other Google identities are never inferred as inboxes, eliminating per-rule source selection and preventing accidental access to members' Gmail accounts.
