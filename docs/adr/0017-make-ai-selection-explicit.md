# Make AI selection explicit

Each Organization selects a default AI Connection and model, and an Automation Rule may explicitly override them. Extraction failures retry on the same connection and become Automation Exceptions rather than silently sending mailbox content to another provider with different privacy, residency, or cost characteristics.
