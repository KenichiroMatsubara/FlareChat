# Retain public attachments until explicit deletion

Public Attachments remain in Drive after an event ends, a recipient declines, or the Calendar event is deleted. Only an authorized member may delete one, after the GUI shows every Scheduled Event and Event Occurrence that references the file and requires explicit confirmation.
