# Use visual message selection policies

Admins define each Automation Rule's Selection Policy in a GUI condition builder supporting nested AND, OR, and NOT expressions over message metadata and content. A rule with no Source List can select all senders only after explicit Admin confirmation, preventing an accidentally broad unattended rule from processing an entire mailbox.
